version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/deploy-board/zip/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: deploy-board.sh
      timeout: 60
      runas: ubuntu